{
  "name": "rd-task-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute rd-task-tracker-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute rd-task-tracker-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.9"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
